{
    "name": "email-capture-worker",
    "version": "1.0.0",
    "description": "Cloudflare Worker for email capture to Airtable",
    "main": "src/index.ts",
    "scripts": {
        "build": "wrangler build",
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "lint": "eslint . --ext .ts",
        "test": "jest"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.0.0",
        "@types/jest": "^29.0.0",
        "@typescript-eslint/eslint-plugin": "^7.0.0",
        "@typescript-eslint/parser": "^7.0.0",
        "eslint": "^8.0.0",
        "jest": "^29.0.0",
        "ts-jest": "^29.0.0",
        "typescript": "^5.0.0",
        "wrangler": "^3.0.0"
    }
}